<%- include('../includes/header.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

            <center>
                <div class="col-sm-4 table-wrapper">
                    <% if(errorMessage) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= errorMessage %>
                        </div>
                        <% } %>
                            <form action="/login" method="POST" novalidate>
                                <div class="form-group row">
                                    <label for="email" class="col-sm-3 col-form-label">E-Mail</label>
                                    <input type="email" name="email" id="email" value="<%= oldInput.email %>" class="form-control col-sm-9 <%= validationErrors.find(e => e.param === 'email') ? 'border-danger' : '' %>">
                                </div>
                                <div class="form-group row">
                                    <label for="password" class="col-sm-3 col-form-label">Password</label>
                                    <input type="password" name="password" id="password" value="<%= oldInput.password %>" class="form-control col-sm-9 <%= validationErrors.find(e => e.param === 'password') ? 'border-danger' : '' %>">
                                </div>
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button class="btn btn-primary col-sm-2" type="submit">Login</button>
                            </form>
                </div>
            </center>
            <%- include('../includes/end.ejs') %>